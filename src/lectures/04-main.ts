export function isPositive(n:number) {
  return n > 0;
}

const count = 1;
console.log(count);

const student = {
  id: 1,
  name: 'nhat',
  isActive: true
}

function getStatus (state: string)  {
  console.log(state);
}

let s = 'active';
getStatus(s); 